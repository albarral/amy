
LOGGING CONFIGURATION

log4cxx configuration files must be located in a .amy folder in user's HOME directory.

Steps to follow:
- create a .amy folder in the home directory
- copy all xml files from this logging directory to that folder.

> 
cd 
mkdir .amy
cp /path_to_amy/logging/*.xml .amy
